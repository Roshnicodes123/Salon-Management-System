<!DOCTYPE html>
<html>
  <head>
    <title>SalonManagementSystem</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %> 
    <%= javascript_importmap_tags %>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
  </head>

  <body>
    <%= yield %>

      <p class="notice"><%= notice %></p>
       <p class="alert"><%= alert %></p>
       <div id="user_nav">
    <% if user_signed_in? %>
        Signed in as <%= current_user.username %>. This cannot be cheese?
        
        <%= link_to 'Sign out', destroy_user_session_path %>
        
        <% elsif barbar_signed_in? %>
        Signed in as <%= current_barbar.username %>. This cannot be cheese?
        <%= link_to 'Sign out', destroy_barbar_session_path %>
    <% else %>
        <%= link_to 'Register As a User', new_user_registration_path %> or <%= link_to 'Sign in as User', new_user_session_path %> ||
         
        <%= link_to 'Register As a Barbar', new_barbar_registration_path %> or <%= link_to 'Sign in as a Barbar', new_barbar_session_path %>
    <% end %>

  </body>
</html>
